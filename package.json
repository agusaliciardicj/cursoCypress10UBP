{
  "name": "cursocypress10ubp",
  "version": "1.0.0",
  "description": "Test de automation con Cypress 10",
  "main": "index.js",
  "scripts": {
    "cypress:open": "npx cypress open --e2e --browser chrome",
    "cypress:run:smoke":  "npx cypress run --e2e --browser chrome -e tags=@smoke",
    "cypress:run:smoke:regression":  "npx cypress run --e2e --browser chrome -e tags=\"@smoke and @regression\""
  },
  "keywords": [
    "cypress",
    "tests"
  ],
  "author": "UBP",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^12.2.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.1.3",
    "cypress": "^10.6.0",
    "cypress-cucumber-preprocessor": "^4.3.1",
    "multiple-cucumber-html-reporter": "^2.0.0"
  }
}
